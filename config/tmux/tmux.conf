# set -g default-terminal "screen-256color"
# set -g default-terminal "xterm-ghostty"

set -g default-terminal "xterm-256color"
# set -g utf8 on

unbind C-b
set -g prefix C-a
bind-key C-a send-prefix

set -sg escape-time 10

set -g mouse on

unbind %
bind '\' split-window -h -c '#{pane_current_path}'
bind '¥' split-window -h -c '#{pane_current_path}'

unbind '"'
bind - split-window -v -c '#{pane_current_path}'

unbind r 
# tmux source-file ~/.tmux.conf \;
bind r source-file ~/.tmux.conf

bind -r j resize-pane -D 5
bind -r k resize-pane -U 5
bind -r h resize-pane -L 5
bind -r l resize-pane -R 5

bind -r m resize-pane -Z

# Copy vim movements
set-window-option -g mode-keys vi

bind-key -T copy-mode-vi 'v' send -X begin-selection
bind-key -T copy-mode-vi 'y' send -X copy-selection

unbind -T copy-mode-vi MouseDragEnd1Pane

# new window
bind c new-window -c '#{pane_current_path}'

# start from 1, not 0
set-option -g base-index 1
# re-number when closing windows
set-option -g renumber-windows on

# open a note for billion $ idea
#bind -r e split-window -h "nvim ~/Documents/git/scratch/notes_$(date +'%Y%m%d%H').md"
#bind -r v split-window -h -c "#{pane_current_path}" "zsh -c 'nvim; exec zsh'"
#bind -r o split-window -h "nvim ~/Documents/git/scratch/todo_$(date +'%y%m%d').md"

# status bar new
set -g status-interval 10

set -g status-style fg=default,bg=default 
set -g status-left '#[fg=colour255,bg=colour60,bold] #S'
set -g window-status-separator ' '
set -g window-status-current-format "#[fg=colour255,bg=colour61,bold] #I:#{b:pane_current_path} #[fg=white]"
set -g window-status-format "#I:#W #[fg=white]"
set -g status-style fg=white,bg=default

# Keep
set -g status-right-length 100 #character length

# eg: 2 sessions, 30 mins work, 5 mins break -> 2,30,5
# bind-key b command-prompt -p "Pomodoro (sessions [work break])" "run-shell '~/nix/dotfiles/tmux/pomocmd.sh %%'"

# bind-key 0 run-shell '~/nix/dotfiles/tmux/pomodoro.sh stop'

set -g status-right '#(curl -s wttr.in?format=%%C+%%t+%%p+%%c)| %d-%m-%Y %H:%M'

# Remind: If you use my .tmux.conf -> remember that you need to add +x mode for all related shell script files

# Smart pane switching with awareness of Vim splits.
# See: https://github.com/christoomey/vim-tmux-navigator
vim_pattern='(\S+/)?g?\.?(view|l?n?vim?x?|fzf)(diff)?(-wrapped)?'
is_vim="ps -o state= -o comm= -t '#{pane_tty}' \
    | grep -iqE '^[^TXZ ]+ +${vim_pattern}$'"
bind-key -n 'C-h' if-shell "$is_vim" 'send-keys C-h'  'select-pane -L'
bind-key -n 'C-j' if-shell "$is_vim" 'send-keys C-j'  'select-pane -D'
bind-key -n 'C-k' if-shell "$is_vim" 'send-keys C-k'  'select-pane -U'
bind-key -n 'C-l' if-shell "$is_vim" 'send-keys C-l'  'select-pane -R'
tmux_version='$(tmux -V | sed -En "s/^tmux ([0-9]+(.[0-9]+)?).*/\1/p")'
if-shell -b '[ "$(echo "$tmux_version < 3.0" | bc)" = 1 ]' \
    "bind-key -n 'C-\\' if-shell \"$is_vim\" 'send-keys C-\\'  'select-pane -l'"
if-shell -b '[ "$(echo "$tmux_version >= 3.0" | bc)" = 1 ]' \
    "bind-key -n 'C-\\' if-shell \"$is_vim\" 'send-keys C-\\\\'  'select-pane -l'"

bind-key -T copy-mode-vi 'C-h' select-pane -L
bind-key -T copy-mode-vi 'C-j' select-pane -D
bind-key -T copy-mode-vi 'C-k' select-pane -U
bind-key -T copy-mode-vi 'C-l' select-pane -R
bind-key -T copy-mode-vi 'C-\' select-pane -l
